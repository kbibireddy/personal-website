/** Tailwind `max-w-4xl` (56rem) widened by 20% */
export const CONTENT_SECTION_MAX_WIDTH = '67.2rem';

/** Scales base typography inside the main content column */
export const CONTENT_FONT_SCALE = 0.9;

/** Tenure label size as a ratio of default `text-xl` (1.25rem), not the scaled size */
export const TENURE_FONT_RATIO = 0.6;

/** Viewport width at which the table of contents is shown */
export const TOC_MIN_VIEWPORT_WIDTH = 1480;

/**
 * Hide the table of contents once the experience section crosses this
 * fraction of the viewport height (from the top).
 */
export const TOC_HIDE_SCROLL_THRESHOLD = 0.45;
